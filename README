Hi, I'm the readme file. Here's what we're doing. We've modified the Drupal vCard module to add some fields, add parameter types to get successful imports into Outlook and added some Drupal profile fields to the vCard.